imports:
    - { resource: services.yml }

framework:
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
    templating:
        engines:
            - twig
    test: true
    validation: false
